import { UrunFiltresiPipe } from './urun-filtresi.pipe';

describe('UrunFiltresiPipe', () => {
  it('create an instance', () => {
    const pipe = new UrunFiltresiPipe();
    expect(pipe).toBeTruthy();
  });
});
